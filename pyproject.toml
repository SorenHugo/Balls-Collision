[project]
name = "balls-collision"
version = "0.1.0"
description = "Simulation of ball to ball collision in Smooth surface."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nuitka>=4.1",
    "pygame-ce>=2.5.6",
]
